# config.py
API_KEY = "your_api_key_here"
API_URL = "https://api.siliconflow.cn/v1/chat/completions"
MODEL_NAME = "deepseek-ai/DeepSeek-R1-0528-Qwen3-8B"

EXCEL_PATH = "examples/一阶段：变量示例_k1.1_20250728.xlsx"
OUTPUT_PATH = "output/0801变量逻辑生成结果_1.xlsx"
DICT_PATH = "examples/CC16_二征征信衍生变量库输入数据字典.xlsx"
FEW_SHOT_PATH = EXCEL_PATH
SHEET_NAME = "算话变量"
START_ROW = 0        # 起始行索引
MAX_ROWS = None      # 处理最大行数，可为 None 表示全部处理
APPENDIX_SHEET = "附录"
DICT_SHEET = "征信合表"

CORE_REQUIREMENTS = """
你是一个熟悉征信系统结构、字段规则、账单周期和变量计算逻辑的专家。你非常清楚如何基于征信报告字段，构造完整且字段一致、逻辑闭环、计算规则明确的变量取值逻辑。
你将根据变量名称、Java 源代码以及字段字典，生成结构化、标准化的中文“取值逻辑说明”
【输出格式要求】请严格按照以下格式输出：
1. 务必使用“字段中文名(字段XML_TAG)”格式说明所用字段，【不要出现】字段英文名或代码变量名；同样的字段中文名如果对应多个字段XML_TAG需要先确认该字段属于哪张表，再取对应的字段XML_TAG；
2. 逻辑结构保持简明、紧凑，不要使用“方法”、“对象”、“属性”等代码术语，不要出现具体代码内容；
2. 明确筛选条件的字段和取值规则；
3. 明确时间范围推导方式（如报告时间(PA01AR01)减去<Px>个月作为起始月，字段为年月格式）；
4. 明确统计字段与其取值编码（如x.getStatusInt() > 0代表还款状态(PD01ED01)为[1-7,G,B,D,Z]的记录，具体见字段编码参考附录）；
5. 明确聚合粒度与方式；
6. 明确特殊筛选规则（如账户状态字段有优先级：优先取[最近一次月度表现信息表.账户状态(PD01CD01)]，如果为空，则取[最新表现信息表.账户状态(PD01BD01))；
7. 不允许使用模糊术语（如“逾期状态”、“币种匹配”等），必须使用征信标准字段与值。
8. 结果最多4步，逻辑必须覆盖代码中的筛选、时间、聚合操作，不要重复描述，不要总结性语言。
"""